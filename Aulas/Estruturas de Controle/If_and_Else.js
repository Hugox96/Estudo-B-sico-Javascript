// O If é usado para excutar um bloco quando uma condição é verdadeira

if(condição = true) {
    // O bloco será executado
}

// O Else é executado quando a condição do if não é verdadeira, em blocos distintos

//Neste caso é uma condição simples, verdadeiro ou falso. Sim ou não.
if(condição = false) {
    // O bloco não sera executado
}

else{
     //O bloco será executado
}